Running the "test" Application (Data Writer)

1. Ensure InfluxDB Cloud is properly configured with the correct credentials.

2. Open a terminalNavigate to the "test" folder:

cd path/to/test

3. Run the application using:

dotnet run


Running the "webapp" Application (Data Visualization)

1. Open a terminaland navigate to the "webapp" folder:

cd path/to/webapp

2. Run the application using the appropriate command:

dotnet run

3. Once the application is running, open a browser and go to:

http://localhost:5080